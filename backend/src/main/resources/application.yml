server:
  port: 8080

storage:
  base-dir: data

spring:
  servlet:
    multipart:
      max-file-size: 512MB
      max-request-size: 512MB
  jackson:
    serialization:
      write-dates-as-timestamps: false
